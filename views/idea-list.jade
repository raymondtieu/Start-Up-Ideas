extends layout

block content
    h4 All Ideas
    ul
        each idea in ideas
            li 
                a(href='/idea=#{idea._id}') #{idea.title}, #{idea.description}, #{idea.industry}
            
    p
        a(href='/new-idea') Propose a new idea

    p
        a(href='/signout') Sign Out

        